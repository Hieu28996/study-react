export interface CommunitiesBoxProps {
	communityType?: string;
}

const CommunitiesBox = (props: CommunitiesBoxProps) => {
	const { communityType } = props;
	return <div>CommunitiesBox</div>;
};

export default CommunitiesBox;
